# OpenProject Docker image tag to test against (override in CI)
openproject.container.tag=17.5.1
openproject.container.shutdown=false

# container runtime settings
openproject.container.port=18088

openproject.allow-header-base-url=false
